import React, { useState } from 'react';


const RoundMaches = (props) => {
  const [numTeams, setNumTeams] = useState(props.numTeams);
  const [matches, setMatches] = useState([]);
  const maches = [];

  
  
}